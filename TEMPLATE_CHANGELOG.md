# Template Changelog

Track changes to the Railway template implementation so consumers can evaluate updates.

## Unreleased

- Added implementation spec aligned to Railway template creation and publish flow.
- Added publish/share instructions, including deploy button integration guidance.
- Added marketplace overview copy in Railway-recommended structure.
- Added validation and checklist refinements for persistence and auth defaults.
- Added Railway-compatible adapter Dockerfile (no Docker `VOLUME`).
- Pinned upstream Paperclip release via `PAPERCLIP_REF` and added automated bump script.

## Changelog format

For each new version, include:

- Date
- What changed
- Why it changed
- Breaking changes (if any)
- Required user actions (if any)
